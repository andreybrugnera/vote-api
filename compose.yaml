services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=vote-db'
      - 'POSTGRES_PASSWORD=${DB_PASSWD}'
      - 'POSTGRES_USER=${DB_USER}'
    ports:
      - '5432:5432'